spring:
  profiles:
    include: common
  cloud:
    config:
      uri: http://127.0.0.1:9899
      profile: dev
      #      discovery:
      #        enabled: true
      #        service-id: config-service
      label: master
eureka:
  client:
    enabled: false