spring:
  profiles:
    include: common
  cloud:
    config:
      uri: http://192.168.31.154:30201
      profile: dev
      #      discovery:
      #        enabled: true
      #        service-id: config-service
      label: master
eureka:
  client:
    enabled: false