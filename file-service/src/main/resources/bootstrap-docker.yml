spring:
  profiles:
    include: common
  cloud:
    config:
      uri: http://192.168.31.154:30201
      profile: docker
      #      discovery:
      #        enabled: true
      #        service-id: config-service
      label: master
#eureka:
#  client:
#    service-url:
#      defaultZone: http://eureka:9999/eureka/
eureka:
  client:
    enabled: false