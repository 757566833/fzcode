spring:
  profiles:
    include: common
  cloud:
    config:
      uri: http://config:9899
      profile: docker
      #      discovery:
      #        enabled: true
      #        service-id: config-service
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://eureka:9999/eureka/