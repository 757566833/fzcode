
server:
  port: 9899
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/fuzhoucode/fzcode-config-center.git
#          strict-host-key-checking: false
          force-pull: true
          clone-on-start: true
          username: fzcode@126.com
          password: fzcode_123
          skipSslValidation: true
#    config:
#      server:
#        native:
#          search-locations: classpath:config/services/auth
#eureka:
#  client:
#    service-url:
#      defaultZone: http://127.0.0.1:9999/eureka/