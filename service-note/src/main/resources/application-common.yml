server:
  port: 30206
spring:
  application:
    name: SERVICE-NOTE
  config:
    import: "optional:configserver:"
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always