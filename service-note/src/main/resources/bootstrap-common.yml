server:
  port: 30206
spring:
  application:
    name: SERVICE-NOTE
  codec:
    max-in-memory-size: 10MB
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always