spring:
  profiles:
    include: common
  cloud:
    config:
      uri: http://config:8888
      profile: docker
      #      discovery:
      #        enabled: true
      #        service-id: config-service
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://eureka:7900/eureka/